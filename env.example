# Cloud: use a PostgreSQL connection string (Supabase Free is suitable for a personal project).
DATABASE_URL=
# Local fallback if DATABASE_URL is empty.
DATABASE_PATH=data/ssc_cgl.db

AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=

# Protect the public deployment. Leave blank only for local-only use.
APP_USERNAME=student
APP_PASSWORD=
EXAM_DATE=2026-10-28
